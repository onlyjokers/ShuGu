{
  "name": "@shugu/node-core",
  "version": "1.0.0",
  "description": "Shared node graph core (types/registry/runtime) for manager and client",
  "main": "./dist-node-core/index.js",
  "module": "./dist-node-core/index.js",
  "types": "./dist-node-core/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist-node-core/index.d.ts",
      "import": "./dist-node-core/index.js"
    }
  },
  "scripts": {
    "build": "pnpm exec tsc",
    "dev": "pnpm exec tsc --watch",
    "clean": "rm -rf dist-node-core",
    "lint": "ESLINT_USE_FLAT_CONFIG=true eslint \"src/**/*.ts\"",
    "test": "pnpm run build && node --test test/*.test.mjs"
  },
  "files": [
    "dist-node-core"
  ],
  "dependencies": {
    "@shugu/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
