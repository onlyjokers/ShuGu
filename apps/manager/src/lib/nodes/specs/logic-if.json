{
  "__purpose": "Manager node spec for logic if (routes a boolean input to true/false outputs based on condition).",
  "type": "logic-if",
  "label": "if",
  "category": "Logic",
  "inputs": [
    { "id": "input", "label": "input", "type": "boolean", "defaultValue": false },
    { "id": "condition", "label": "condition", "type": "boolean", "defaultValue": false }
  ],
  "outputs": [
    { "id": "false", "label": "false", "type": "boolean" },
    { "id": "true", "label": "true", "type": "boolean" }
  ],
  "configSchema": [],
  "runtime": { "kind": "logic-if" }
}
