{
  "type": "proc-visual-scene-front-camera",
  "label": "Legacy Visual Scene-Front Camera",
  "category": "Legacy",
  "inputs": [
    { "id": "enabled", "label": "Enabled", "type": "boolean", "defaultValue": false }
  ],
  "outputs": [{ "id": "cmd", "label": "Cmd", "type": "command" }],
  "configSchema": [
    { "key": "enabled", "label": "Enabled", "type": "boolean", "defaultValue": false }
  ],
  "runtime": {
    "kind": "command",
    "command": {
      "action": "visualSceneFrontCamera",
      "payload": {
        "enabled": {
          "kind": "booleanFromThreshold",
          "inputKey": "enabled",
          "configKey": "enabled",
          "threshold": 0.5,
          "default": false
        }
      }
    }
  }
}
