{
  "__purpose": "Manager-only node: attach to a Group frame as an activation gate input (bool) to enable/disable the group's nodes.",
  "type": "group-activate",
  "label": "Activate",
  "category": "Internal",
  "inputs": [{ "id": "active", "label": "Active", "type": "boolean", "defaultValue": true }],
  "outputs": [],
  "configSchema": [{ "key": "groupId", "label": "Group ID", "type": "string", "defaultValue": "" }],
  "runtime": { "kind": "group-activate" }
}
