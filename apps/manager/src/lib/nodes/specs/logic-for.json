{
  "__purpose": "Manager node spec for logic for (emits an integer index from start..end, advancing one step per tick).",
  "type": "logic-for",
  "label": "for",
  "category": "Logic",
  "inputs": [
    { "id": "start", "label": "start", "type": "number", "defaultValue": 1, "min": 1, "step": 1 },
    { "id": "end", "label": "end", "type": "number", "defaultValue": 1, "min": 1, "step": 1 }
  ],
  "outputs": [{ "id": "index", "label": "index", "type": "number" }],
  "configSchema": [],
  "runtime": { "kind": "logic-for" }
}
